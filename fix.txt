The desktop version of this site looks broken. I need you to properly redesign the layout for desktop screens (1024px and above). This is not a CSS tweak job, do it properly.
What needs to be built for desktop:

Create a new css/desktop.css file and link it in every HTML file inside a media query link tag so it only loads on desktop. Do not touch the existing css/styles.css at all, mobile must stay exactly as is.
Top navigation bar - sticky, dark surface colour #1a2332, logo left, links right. IT Services and Marketing have hover dropdowns showing all child pages. A teal "Get in touch" button far right. Hide the mobile bottom nav completely on desktop.
Every page gets a proper desktop layout:

Max content width 1200px, centered
Home hero: large full-width hero with headline left, a visual element or stats right
IT and Marketing hubs: service cards in a 3-column grid
Service detail pages: two-column layout, content left, sticky CTA card right
About page: stats in a 4-column row, values in a 2-column grid
Contact page: three contact buttons side by side horizontally


Keep all existing colours, fonts, dark mode, copy and file structure exactly as is. Only add the desktop stylesheet, nothing else.

After creating css/desktop.css, go through every single HTML file and add this line inside the <head> tag, after the existing stylesheet link:
<link rel="stylesheet" media="(min-width: 1024px)" href="/css/desktop.css">
Do this on every HTML file without exception. The mobile stylesheet loads always, the desktop stylesheet only loads when the screen is 1024px or wider.